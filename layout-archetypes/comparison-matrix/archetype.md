# comparison-matrix

This archetype defines a reusable slide structure for creative editable PPTX generation. This package is HTML/replica adapter metadata selected through `data-archetype`; creative text uses the compiled `deck.plan.json` registry.

Use this layout when the slide role matches the required slots in `schema.json`.
