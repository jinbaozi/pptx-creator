# comparison-matrix

This archetype defines a reusable slide structure for creative editable PPTX generation. It should be selected by `slide-design-specs.json` through the `layoutType` field.

Use this layout when the slide role matches the required slots in `schema.json`.
