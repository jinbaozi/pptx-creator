# quote

This archetype defines a large pull-quote slide with attribution. This package is HTML/replica adapter metadata selected through `data-archetype`; creative text uses the compiled `deck.plan.json` registry.

Use this layout when one statement deserves the full slide. Pair with attribution that names the speaker (or source) and their role. Do not use this layout for ordinary claims that fit a bullets-list.
