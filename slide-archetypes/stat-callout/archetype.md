# stat-callout

This archetype defines a single hero metric plus supporting text (one big number per slide). This package is HTML/replica adapter metadata selected through `data-archetype`; creative text uses the compiled `deck.plan.json` registry.

Use this layout when a single number is the entire point of the slide. Multi-metric rows belong in the numbers-row content pattern (which composes stat-callout horizontally).
