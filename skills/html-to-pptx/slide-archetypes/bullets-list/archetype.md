# bullets-list

This archetype defines a single-column bulleted list of points (4-7 items). This package is HTML/replica adapter metadata selected through `data-archetype`; creative text uses the compiled `deck.plan.json` registry.

Use this layout when a single claim needs 4-7 supporting points that share the same weight. No icons are required; if used, every bullet must carry the same icon treatment.
