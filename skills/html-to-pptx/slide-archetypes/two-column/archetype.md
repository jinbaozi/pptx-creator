# two-column

This archetype defines two equal-width columns of body text or list. Use this archetype in the HTML source by setting `data-archetype` on the slide section; convert that HTML directly or provide it with optional `presentation-package.json` metadata.

Use this layout when the slide needs two coherent columns of comparable weight. Avoid using it for layout decoration; if the columns are unrelated, split the slide.
