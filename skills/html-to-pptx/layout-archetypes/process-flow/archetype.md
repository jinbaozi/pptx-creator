# process-flow

This archetype defines a reusable slide structure for creative editable PPTX generation. Use this archetype in the HTML source by setting `data-archetype` on the slide section; convert that HTML directly or provide it with optional `presentation-package.json` metadata.

Use this layout when the slide role matches the required slots in `schema.json`.
