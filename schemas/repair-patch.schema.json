{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pptx-creator.local/repair-patch.schema.json",
  "title": "Repair Patch",
  "type": "object",
  "required": ["attempt", "patches"],
  "properties": {
    "attempt": { "type": "integer", "minimum": 1, "maximum": 3 },
    "patches": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["slideId", "operation", "targetElementId", "changes"],
        "properties": {
          "slideId": { "type": "string" },
          "operation": {
            "type": "string",
            "enum": ["move", "resize", "updateStyle", "updateText", "removeElement", "increaseSpacing", "reduceDensity", "adjustStyle"]
          },
          "targetElementId": { "type": "string" },
          "changes": { "type": "object" }
        }
      }
    }
  }
}
